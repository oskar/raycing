export default class Map {
  constructor(size) {
    this.positions = [];
    for(var i = 0; i < size * size; i++) {
      this.positions.push({exists: false});
    }
  }

  addCircle(pos, radius) {
    this.positions[]
  }
}
